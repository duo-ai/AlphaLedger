interface:
  display_name: "TDD Workflow"
  short_description: "Drive a unit with its test list before implementation"
  brand_color: "#22C55E"
  default_prompt: "Use $tdd-workflow to drive the unit with its test list before implementation."
policy:
  allow_implicit_invocation: true
